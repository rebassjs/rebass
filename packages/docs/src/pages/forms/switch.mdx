export const title = 'Switch'

# Switch

Toggle switch button component

```js
import { Switch } from '@rebass/forms'
```

```jsx live
<Switch />
```

Note that the `Switch` component uses the DOM `button` tag for accessibility. Thus, it will not accept an `onChange` attribute, but rather an `onClick` attribute.
