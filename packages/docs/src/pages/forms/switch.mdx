export const title = 'Switch'

# Switch

Toggle switch button component

```js
import { Switch } from '@rebass/forms'
```

```jsx live
<Switch />
```

Switch is an uncontrolled component, you can use the `checked` and `onClick` props to hook it into state
